import { FindingType, FindingSeverity, Finding, HandleTransaction } from "forta-agent";

describe("Large add/remove Liquity on Pairs agent tests suite", () => {
  it("should do something", async () => {});
});
