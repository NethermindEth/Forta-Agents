import {
  FindingType,
  FindingSeverity,
  Finding,
  HandleTransaction,
  createTransactionEvent,
} from "forta-agent";
import agent from "./agent";

describe("Pools RampA tests suite", () => {
  it("some test", async () => {
    
  });
});
