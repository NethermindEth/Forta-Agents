import {
  FindingType,
  FindingSeverity,
  Finding,
  HandleTransaction,
  createTransactionEvent,
} from 'forta-agent';
import agent from './agent';

describe('Protocol Specific Alerts Agent Test Suite', () => {
  let handleTransaction: HandleTransaction;
});
