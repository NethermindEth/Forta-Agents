import {
  FindingType,
  FindingSeverity,
  Finding,
  HandleTransaction,
  createTransactionEvent
} from "forta-agent";
import agent from "./agent";

describe("Large Deposit/Withdraw agent test suite", () => {
  it("handleTransaction", () => {});
});
